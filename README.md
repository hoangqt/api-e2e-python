![Random picture from my walk](sky.png)

*To see things as they are. Substance, cause and purpose. - Marcus Aurelius*

## Summary

A simple Astral `uv` project for testing a subset GitHub API using REST
A simple project, based on Astral's uv, for testing a subset of the GitHub API using REST requests.

### Local setup

- Add `github-pat`="<your-github-pat>" to `tests/resources/config.toml`
- Run `uv run pytest tests/` to execute the tests
